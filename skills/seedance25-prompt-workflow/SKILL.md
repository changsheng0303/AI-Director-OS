---
name: seedance25-prompt-workflow
description: "按即梦 Seedance 2.5 规范逐项收集参数并生成提示词，支持基础/真人、30 秒与超长视频、延长、智能或高级编辑、白模和多宫格分镜。"
---

# Seedance 2.5 提示词全流程生成器

## 功能定位

根据即梦 Seedance 2.5 官方使用手册，以**勾选式 UI（向用户提出简短问题）**逐项引导用户完成提示词创作。

## 交互铁律

1. **每轮都用 向用户提出简短问题**。单选用 `multiSelect: false`，多选用 `multiSelect: true`。
2. **每轮最多 4 个问题，每题最多 4 个选项**。
3. **需要自由输入时**：选项列表中加一个 `"我来输入（在 Other 框写出）"`，用户勾选后会在 Other 框写自定义内容。
4. **选项要具体、可操作**：不是"你选什么场景"而是"雨夜霓虹街道 / 古风烛火书房 / 阳光海滩 / 我来输入"。
5. **逐轮推进**：不一次性抛出所有问题。每轮收集完 → 分析答案 → 下一轮。
6. **收集完毕才生成**：所有必填项确认后，输出最终提示词，用 Markdown 代码块包裹。

---

## 转场选择流程（共享模块）

**所有多镜头/多场景类型，在确定切镜方案后，必须插入此轮问答：**

### 转场 R-A：选择方式（向用户提出简短问题，单选）
| # | header | 选项 |
|---|--------|------|
| 1 | 转场 | 你帮我选最合适的（根据风格自动匹配） / 我自己指定 |

→ 选"你帮我选"：提示词中用模糊转场指令 `请从[自然镜头切换/叠化/水墨转场/...]中根据本片风格选择最合适的一种`。

→ 选"我自己指定"：进入 R-B，分两轮展示全部 11 种。

### 转场 R-B：第一组（向用户提出简短问题，单选/多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 转场 | 自然镜头切换（镜头呼吸感、无过渡） / 淡入淡出（渐黑/渐白） / 叠化（两画面重叠交融） / 白闪/黑闪（瞬间闪白、强冲击） |

→ Other = 继续看。

### 转场 R-C：第二组（向用户提出简短问题，单选/多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 转场 | 擦除（推拉门式，复古/地点转移） / 遮罩转场（被物遮挡→变黑→新场景） / 相似物转场（形状/色彩相似无缝过渡） / 动作转场（甩动模糊→切入、速度感） |

→ Other = 继续看。

### 转场 R-D：第三组（向用户提出简短问题，单选/多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 转场 | 动态接力（跳出画框→落入新场景，换装/旅行） / 拉近/拉远（放大细节→变新场景全貌） / 水墨转场（墨迹晕染→东方美学） |

→ 11 种转场全部覆盖。用户确认每个镜头间用什么转场，最终写入提示词的切镜/转场公式中。

---

## 质感判断流程（共享模块）

**所有涉及人物的类型，在角色设定完成后，必须插入此轮问答：**

### 质感 R-A：真实感级别（向用户提出简短问题，单选）
| # | header | 选项 |
|---|--------|------|
| 1 | 质感 | 真人电影级（原生皮肤/毛发分明/去AI味） / 动漫/二次元 / 3D渲染/CG / 无所谓/默认 |

→ 选"真人电影级"：提示词自动加入 `原生皮肤质感`、`毛发根根分明`、`自然的神态/去AI味`。
→ 选"动漫/二次元"：提示词加入 `二次元动画风格`。
→ 选"3D渲染/CG"：提示词加入 `3D渲染质感`。

**铁律：涉及人物的类型（基础提示词有人物、30秒、超长、多宫格），此轮不可跳过。**

---

## 时长规划流程（共享模块）

**所有叙事类类型，在场景和动作收集完毕后，必须插入此轮问答：**

### 时长 R-A：预期时长（向用户提出简短问题，单选）
| # | header | 选项 |
|---|--------|------|
| 1 | 预期时长 | 5秒短片段 / 8-10秒 / 12-15秒 / 你根据内容定 |

→ 此轮仅用于规划镜头数量和叙事节奏，时长不写入提示词（即梦界面上直接选）。

**铁律：基础提示词、30秒、超长、多宫格类型，此轮不可跳过。**

---

## 禁止项确认流程（共享模块）

**所有类型，在生成提示词前的最后一轮，必须插入此问答：**

### 禁止 R-A：禁止项（向用户提出简短问题，多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 禁止 | 字幕/文字 / 水印Logo / AI塑料感/过度磨皮 / 背景音乐 / 夸张表情 / 我来输入 |

→ 收集结果直接写入提示词的全局约束/禁止项段落。

**铁律：所有类型（含基础、30秒、超长、延长、编辑、白模、多宫格），此轮不可跳过。**

---

## 核心公式速查（内部参考，生成时使用）

当任务是高风险单镜头或少量连续镜头，且成败主要取决于首帧占位、角色站位、视线、地标距离、道具接触、物理重量、光线方向或连续性时，读取 [single-shot-physical-control.md](references/single-shot-physical-control.md)。该参考只增强内容控制，不替代本 Skill 当前的 Seedance 2.5 格式和用户已选参数。

### 分效果提示词五模块（叙事类：基础/30秒/超长/延长）
1. **镜头描述公式**：`[景别] + [运镜] + [角度] + [焦距]`
2. **切镜/转场公式**：`[转场类型] + [基础约束] + [切镜逻辑]`
3. **动作公式**：`[角色绑定] + [起始限制] + [连贯动作链] + [细节节奏]`
4. **对白公式**：`[角色+情绪] + [台词] + [语气] + [音色约束]`
5. **全局约束公式**：`[风格] + [参考声明] + [禁止项]`

### 编辑公式
- **智能编辑**：`[修改对象] + [变化描述] + [生效时序]`
- **高级编辑**：`[标注工具+方位] + [修改对象] + [变化描述] + [生效时序]`

### 白模公式
- **粗颗粒度**：`【参考声明】→【对应关系映射】→【剧情详述】→【场景处理】→【收束】`
- **细颗粒度**：`【渲染指令】→【分段渲染描述】→【场景处理】→【收束】`

### 多宫格公式
`[分镜指代] → [主体设定] → [逐镜故事+过渡] → [全局风格+禁止项]`

### 11种转场速查
自然镜头切换 / 淡入淡出 / 叠化 / 白闪黑闪 / 擦除 / 遮罩转场 / 相似物转场 / 动作转场 / 动态接力转场 / 拉近拉远转场 / 水墨转场

---

## 执行流程

### 阶段 0：选择类型（向用户提出简短问题，单选）

用 向用户提出简短问题 让用户勾选：

**第一轮（4 个）：**
| header | 选项 |
|--------|------|
| 类型 | 基础提示词（任意题材短片段） |
| | 真人人物（电影级真实感） |
| | 30秒长视频（多镜头分效果体系） |
| | 超长视频（30-180秒一次生成） |

如果用户选 Other → **第二轮（5 个）：**
| header | 选项 |
|--------|------|
| 类型 | 视频延长（续写已有视频） |
| | 智能编辑（文字描述局部编辑） |
| | 高级编辑/视频编辑（框选精细编辑） |
| | 白模（3D白模动画生成） |
| | 多宫格分镜（分镜图转视频） |

---

### 类型 1：基础提示词

#### R1：题材与风格（向用户提出简短问题，单选）
| # | header | 选项 |
|---|--------|------|
| 1 | 题材 | 古风武侠 / 现代都市 / 科幻赛博 / 我来输入 |
| 2 | 情绪 | 压抑克制 / 浪漫温柔 / 燃炸激烈 / 我来输入 |

#### R2：角色（向用户提出简短问题，单选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 角色性别 | 男 / 女 / 无特定 |
| 2 | 年龄层 | 少年 / 青年 / 中年 / 我来输入 |
| 3 | 有参考图？ | 有（后续发@引用） / 没有（纯文字描述） |

→ 追问外观（Other 框）：简要描述发型/服装/体型/特殊特征。

→ 如果用户在 R2 选了"无特定"（无人物）：跳过 R_质感，直接进入 R3。

#### R_质感：质感判断 → 使用 **质感判断流程**（共享模块）

**不可跳过。**

#### R3：场景（向用户提出简短问题，多选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 时间 | 白天 / 夜晚 / 黄昏/黎明 |
| 2 | 地点 | 室内 / 室外 / 两者都有 |
| 3 | 环境元素 | 雨 / 雪 / 霓虹 / 烛火 / 废墟 / 森林 / 我来输入 |

→ 追问氛围细节（Other 框）：场景中还有什么关键元素？

#### R4：动作与道具（向用户提出简短问题，多选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 动作类型 | 静态表演（微表情/对白） / 慢速动态（走路/转身） / 快速动态（战斗/舞蹈/跑酷） |
| 2 | 有台词？ | 有（请在 Other 框输入台词） / 没有 |
| 3 | 有道具？ | 有（请在 Other 框描述道具） / 没有 |

#### R_时长：时长规划 → 使用 **时长规划流程**（共享模块）

**不可跳过。** 此轮仅用于规划镜头数量与叙事节奏，不写入提示词。

#### R5：镜头（向用户提出简短问题，单选/多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 景别 | 特写 / 近景 / 中景 / 我来输入 |
| 2 | 运镜 | 固定 / 缓慢推拉 / 环绕 / 手持跟拍 / 我来输入 |

#### R6：声音与切镜（向用户提出简短问题，单选/多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 声音 | 纯净无声 / 仅环境音 / 需要BGM（Other框写风格） |
| 2 | 切镜？ | 单镜头一镜到底 / 多镜头切换 |

→ 如果选"多镜头切换" → 先追问：**镜头数量**（向用户提出简短问题，单选）：
| # | header | 选项 |
|---|--------|------|
| 1 | 镜头数 | 2-3个 / 4-5个 / 6个以上 / 你来定 |

→ 然后插入 **转场选择流程**（共享模块），每个镜头间确认转场方式。收集完毕后继续 R7。

→ 追问镜头偏好（Other 框）：运镜偏好？景别偏好？不答则我来定。

#### R7：分效果模式 + 禁止项（向用户提出简短问题，单选 + 多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 禁止项 | 字幕/文字 / 水印Logo / AI塑料感 / 背景音乐 / 夸张表情 / 我来输入 |
| 2 | 复杂度 | 单一简单镜头 → 简洁版 / 多镜头或复杂叙事 → 分效果提示词 |

→ 收集完毕，生成提示词。

---

### 类型 2：真人人物

#### R1：人物数量与外观（向用户提出简短问题，单选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 人物数量 | 单人 / 双人 / 三人及以上 |
| 2 | 角色关系 | 恋人 / 对手 / 陌生人 / 我来输入 |
| 3 | 有参考图？ | 有（后续@引用） / 没有 |

→ 追问（Other 框）：主角外观（性别/年龄/发型/服装/体型/肤色）。

#### R2：真实感级别（向用户提出简短问题，多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 质感要求 | 原生皮肤质感 / 毛发根根分明 / 自然神态/去AI味 / 服装面料纹理 |
| 2 | 物理细节 | 汗水/雨水 / 头发飘动 / 微表情 / 我来输入 |

#### R3：动作与表演（向用户提出简短问题，单选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 动作类型 | 静态表演（表情/微动作） / 慢速动态（走路/转身） / 复杂动作（武术/舞蹈/运动） |
| 2 | 表演风格 | 克制内敛 / 自然生活化 / 戏剧化 |
| 3 | 具体动作 | 我来输入（在 Other 框详细描述） |

#### R4：场景与镜头（向用户提出简短问题，单选/多选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 场景地点 | 室内 / 室外 / 我来输入 |
| 2 | 光源类型 | 自然光 / 烛火灯光 / 霓虹 / 我来输入 |
| 3 | 切镜方式 | 单镜头一镜到底 / 多镜头切换 |

→ 如果选了"多镜头切换" → 插入 **转场选择流程**（共享模块）。

#### R5：声音确认（向用户提出简短问题，单选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 台词 | 有（Other 框输入内容+语气） / 没有 |
| 2 | 声音环境 | 纯净无BGM / 仅环境音 / 需要BGM+音效 |

→ 收集完毕。

#### R6：禁止项（向用户提出简短问题，多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 禁止 | AI脸/塑料质感 / 夸张表情 / 字幕文字 / 背景音乐 / 我来输入 |

→ 收集完毕，生成提示词。核心关键词自动加入：原生皮肤质感、毛发根根分明、自然的神态。

---

### 类型 3：30秒长视频

#### R1：全局设定（向用户提出简短问题，单选/多选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 题材 | 古风 / 现代 / 科幻 / 我来输入 |
| 2 | 角色数量 | 单人 / 双人 / 多人 |
| 3 | 情绪基调 | 史诗 / 压抑 / 浪漫 / 我来输入 |

→ 追问（Other 框）：全片视觉风格？有参考素材吗？

#### R_质感：质感判断 → 使用 **质感判断流程**（共享模块）

**不可跳过。**

#### R2：分镜头规划（向用户提出简短问题 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 镜头数 | 2-3个 / 4-5个 / 6个以上 |

→ 追问（Other 框）：按顺序列出每个镜头的时间范围和一句话内容。

#### R3：每个镜头逐一确认（向用户提出简短问题，每个镜头一轮）

对镜头X：
| # | header | 选项 |
|---|--------|------|
| 1 | 景别 | 特写 / 近景 / 中景 / 全景 |
| 2 | 运镜 | 固定 / 推/拉 / 摇/移 / 环绕 |
| 3 | 角度 | 平视 / 仰拍 / 俯拍 / 过肩 |

→ 追问（Other 框）：这个镜头中角色做什么？有关键道具吗？

#### R4：动作与台词（向用户提出简短问题 + Other，逐镜确认）
每个镜头：
| # | header | 选项 |
|---|--------|------|
| 1 | 有台词？ | 有（Other 框输入内容+语气） / 没有 |
| 2 | 有道具？ | 有（Other 框描述道具+物理轨迹） / 没有 |

#### R5：切镜与转场 → 使用 **转场选择流程**（共享模块）

→ 30秒长视频每个镜头间都必须确认转场方式。如果用户说"每个镜头间用不同的"，逐对询问。

#### R6：声音设计（向用户提出简短问题，多选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 音效 | 环境音（雨/风/街） / 动作音效 / 我来输入 |
| 2 | 背景音乐 | 无BGM / 有BGM（Other 框写风格） |

#### R7：约束与禁止（向用户提出简短问题，多选 + 单选）
| # | header | 选项 |
|---|--------|------|
| 1 | 时间戳 | 使用 `[X-Y秒]` 精确控制 / 不强制 |

→ 然后插入 **禁止项确认流程**（共享模块）。

→ 收集完毕。

---

### 类型 4：超长视频（30-180秒）

#### R1：时长规划（向用户提出简短问题，单选）
| # | header | 选项 |
|---|--------|------|
| 1 | 总时长 | 30秒 / 60秒 / 90秒 / 120秒 / 150秒 / 180秒 |
| 2 | 分段数 | 2段 / 3段 / 4段+（Other 框写出每段时间范围和主题） |

#### R2：角色与一致性（向用户提出简短问题，单选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 主角数量 | 单人 / 双人 / 多人 |
| 2 | 场景变化 | 单一场景贯穿 / 多场景切换 |
| 3 | 有参考图？ | 有（后续@引用） / 没有 |

→ 追问（Other 框）：主角外观特征（必须全程保持一致）。

#### R_质感：质感判断 → 使用 **质感判断流程**（共享模块）

**不可跳过。**

#### R3：逐段内容（向用户提出简短问题 + Other，每段一轮）
对阶段X（X-Y秒）：
| # | header | 选项 |
|---|--------|------|
| 1 | 镜头数 | 1个 / 2-3个 / 4个+ |
| 2 | 情感曲线 | 上升（紧张加剧） / 下降（松弛舒缓） / 平稳 / 起伏 |

→ 追问（Other 框）：这段的核心画面和动作是什么？

→ 如果场景有切换（多个不同的场景） → 插入 **转场选择流程**（共享模块），确认场景间转场方式。

#### R4：声音（向用户提出简短问题，单选/多选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | BGM | 无 / 有（Other 框写风格） |
| 2 | 台词 | 无 / 有（Other 框写时间段+内容） |
| 3 | 环境音 | 有 / 无 |

#### R5：全局约束（向用户提出简短问题，多选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 视觉风格 | 我来输入（Other 框） |

→ 然后插入 **禁止项确认流程**（共享模块）。

→ 收集完毕。

---

### 类型 5：视频延长

#### R1：原视频（向用户提出简短问题，单选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 原视频时长 | 4-10秒 / 11-20秒 / 21-30秒 |
| 2 | 来源 | 已生成（@引用） / 需上传（Other 框给路径） |

→ 追问（Other 框）：原视频结尾画面是什么？（人物+场景+情绪）

#### R2：延长方式（向用户提出简短问题，单选）
| # | header | 选项 |
|---|--------|------|
| 1 | 延长方式 | 一次性延长 / 套娃式多次延长 |
| 2 | 增加时长 | 4-10秒 / 11-20秒 / 21-30秒 |

→ 如果是套娃式：追问每次延长的秒数和内容（逐段收集）。

#### R3：延长内容（向用户提出简短问题 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 新增动作 | 延续原视频动作 / 转折/反转 / 加入新角色 / 场景穿越 |
| 2 | 衔接方式 | 自然无痕衔接 / 有明确转场 |

→ 追问（Other 框）：延长部分的具体画面描述。

#### R4：声音与参数（向用户提出简短问题，单选/多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 新增台词 | 有（Other 框输入） / 没有 |
| 2 | 新增音效 | 有（Other 框输入） / 没有 |

→ 然后插入 **禁止项确认流程**（共享模块）。

→ 收集完毕。

---

### 类型 6：智能编辑

#### R1：编辑目标（向用户提出简短问题，单选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 视频来源 | 已生成（@引用） / 需上传（Other 框写路径） |
| 2 | 编辑类型 | 增加/加上 / 删除/去掉 / 修改/替换 / 局部消除 / 绿幕替换 |

#### R2：编辑对象（向用户提出简短问题 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 修改范围 | 单个物体/人物 / 画面背景 / 服装/配饰 / 我来输入 |

→ 追问（Other 框）：具体修改什么对象？（如："男生的牛仔裤"）→ 变成什么？（如："替换为深黑色西装裤"）

#### R3：生效范围（向用户提出简短问题，单选）
| # | header | 选项 |
|---|--------|------|
| 1 | 时间范围 | 全片统一修改 / 仅第X-Y秒 / 从第X秒起逐渐显现 |
| 2 | 额外参考 | 需要参考图 / 需要参考音频 / 不需要 |

→ 收集完毕。按公式 `[修改对象] + [变化描述] + [生效时序]` 输出。

---

### 类型 7：高级编辑/视频编辑

#### R1：编辑入口（向用户提出简短问题，单选）
| # | header | 选项 |
|---|--------|------|
| 1 | 功能 | 高级编辑（本地上传+标注工具） / 视频编辑（已生成视频的再编辑） |

#### R2：标注工具（向用户提出简短问题，多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 工具 | 红框 / 蓝框 / 箭头 / 直线 / 地标点 / 画笔涂抹 / 文字标注 |

#### R3：标注方位（向用户提出简短问题 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 关注范围 | 标注工具「里面」 / 标注工具「外面」 |

→ 追问（Other 框）：标注工具具体画在哪里？（如："红框圈出男生裤子"）

#### R4：编辑内容（向用户提出简短问题，单选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 操作类型 | 替换 / 消除 / 增加 / 修改 |

→ 追问（Other 框）：把什么变成什么？（如："替换为深黑色西装裤"）

#### R5：生效时序（向用户提出简短问题，单选）
| # | header | 选项 |
|---|--------|------|
| 1 | 时间范围 | 全片统一 / 仅第X-Y秒 / 从第X秒起逐渐显现 |
| 2 | 保持不变 | 动作轨迹不变 / 画风不变 / 运镜不变 / 我来输入 |

→ 收集完毕。按公式 `[标注工具+方位] + [修改对象] + [变化描述] + [生效时序]` 输出。

---

### 类型 8：白模

#### R1：白模类型（向用户提出简短问题，单选）
| # | header | 选项 |
|---|--------|------|
| 1 | 白模类型 | 粗颗粒度（简单几何体，只做动态骨架） / 细颗粒度（建模完整，做材质渲染） |

#### R2：素材（向用户提出简短问题，多选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 已有素材 | 白模参考视频 / 角色参考图 / 场景参考图 / 参考音频 |

→ 追问（Other 框）：请发送素材（@引用或路径）。

#### ——分支 A：粗颗粒度——

#### R3A：参考声明（向用户提出简短问题，多选）
| # | header | 选项 |
|---|--------|------|
| 1 | 参考白模的 | 运镜 / 动作 / 动线 / 光影变化 / 角色站位 / 画面节奏 |
| 2 | 对应映射 | 我来输入（Other 框：白模中 XX模型 = 参考图X的XX角色） |

#### R4A：剧情描述（向用户提出简短问题 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 场景来源 | 纯文字描述 / 参考图（@图X的XX场景） |

→ 追问（Other 框）：按时间线描述完整剧情（动作/表情/台词/光影变化）。

#### R5A：收束（向用户提出简短问题，多选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 画质 | 4K电影级 / 高品质 / 标准 |
| 2 | 额外要求 | 人物一致性 / 场景一致性 / 去除音轨干扰 / 我来输入 |

→ 追问（Other 框）：整体风格描述（如：赛博朋克 / 宫崎骏奇幻 / 纪实写实）。白模中是否有四肢/翅膀模型？如有需写出完整动作序列。

#### ——分支 B：细颗粒度——

#### R3B：渲染指令（向用户提出简短问题 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 风格方向 | 赛博朋克 / 写实 / 卡通渲染 / 像素风 / 水墨 / 我来输入 |

→ 追问（Other 框）：补充说明——上传前是否去除了轨迹线/坐标线/相机cone等干扰信息？

#### R4B：分段渲染（向用户提出简短问题 + Other）
分段描述每段的渲染要求（Other 框：0-Xs背景环境/色调/材质/光影 → X-Ys转场触发点与新场景渲染）

#### R5B：收束（同 R5A）

→ 收集完毕。按对应公式输出。

---

### 类型 9：多宫格分镜

#### R1：分镜图（向用户提出简短问题，单选 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 分镜类型 | 火柴人线稿 / 简约线稿 / 我来输入 |
| 2 | 格子数 | 2格 / 4格 / 6格 / 我来输入 |

→ 追问（Other 框）：请上传分镜图（@引用或路径）。

#### R2：分镜指代（向用户提出简短问题 + Other）
→ 追问（Other 框）：每个格子代表什么？（格1=XX / 格2=XX / ...）

#### R3：主体设定（向用户提出简短问题 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 角色数量 | 单人 / 双人 / 多人 |
| 2 | 有参考图？ | 有（后续@引用） / 没有 |

→ 追问（Other 框）：主角外观 + 其他角色外观 + 重要道具。

#### R_质感：质感判断 → 使用 **质感判断流程**（共享模块）

**不可跳过。**

#### R_时长：时长规划 → 使用 **时长规划流程**（共享模块）

**不可跳过。** 此轮仅用于规划每格对应的时间分配，不写入提示词。

#### R4：逐镜描述（向用户提出简短问题 + Other，每个格子一轮）
对格X：
→ 追问（Other 框）：景别？运镜？画面核心内容？角色动作？台词？

#### R5：过渡与风格（向用户提出简短问题 + Other）
| # | header | 选项 |
|---|--------|------|
| 1 | 过渡方式 | 你帮我选最合适的 / 我自己指定 |

→ 选"我自己指定" → 插入 **转场选择流程**（共享模块），逐格之间确认转场。

→ 追问（Other 框）：格与格之间的过渡画面。全片统一视觉风格？

→ 然后插入 **禁止项确认流程**（共享模块）。

→ 收集完毕。按多宫格公式输出。

---

## 通用功能关键词（生成时按需加入）

| 功能 | 关键词 |
|------|--------|
| 去除BGM | "去除背景音乐"、"删除BGM，仅保留人物对白" |
| 无字幕 | "纯净视频，无字幕"、"画面禁字幕/文字/水印/Logo" |
| 真人质感 | "原生皮肤质感"、"毛发根根分明"、"自然的神态" |
| 时间戳 | `[X-Y秒]` 标记在动作/镜头前；**多片段项目一律用片段相对时间（从0s起算），全片位置标注在提示词外** |
| 迁移创意 | "@参考视频 + 迁移其镜头语言/情绪氛围/网感节奏" |
| 多语种 | 直接用目标语言写台词 |
| 一致性 | "全片人物外貌/服装/场景保持一致，禁止变异" |
| 绿幕 | "将背景替换为XX场景" |
| 多人独立 | "人物A与B保持各自面部特征，禁止长相趋同" |
| 空间视角 | "将平视修改为高空俯拍" |
| 模糊转场 | "请从[转场A/B/C]中根据本片风格选择最合适的" |

---

## 输出格式（关键！）

**最终提示词必须按以下格式输出，不可省略任何一步：**

1. **Markdown 代码块**：用 ` ```txt ` 包裹（必须带语言标记 `txt`，否则右上角不出现复制按钮）。代码块前后各空一行。
2. **同步输出文件**：将提示词写入 `outputs/` 目录（.txt 文件），然后用 present_files 展示——双保险。

代码块前加一行说明：`[X-Y秒]` 控制时间轴；`@图N` / `@视频N` / `@音频N` 引用素材。**提示词中禁止写画幅/分辨率/时长——这些在即梦界面上直接选。** 多片段项目：每段提示词时间戳从本片段 0s 起算，全片位置单独标注（见铁律「时间戳相对化」）。

示例输出格式：
````
已收集全部参数，生成提示词：

```txt
提示词内容...
```
````

## 铁律

- **逐轮勾选**：每轮用 向用户提出简短问题，最多 4 题、每题最多 4 选项，等用户回答后再下一轮。
- **按类型分支**：选择类型后严格按该类型模板执行。
- **四大必问轮次（不可跳过）**：
  1. **质感判断**：所有涉及人物的类型，角色设定完成后必问。真人电影级 / 动漫 / 3D / 无所谓。
  2. **时长规划**：所有叙事类类型，场景和动作收集完毕后必问。仅用于规划镜头数量和节奏，不写入提示词。
  3. **镜头数量**：选"多镜头切换"后必问。2-3个 / 4-5个 / 6个+ / 你来定。
  4. **禁止项**：所有类型生成前的最后一轮必问。字幕/水印/AI塑料感/BGM/夸张表情/自定义。
- **收集完毕才生成**：所有必填项确认后才输出提示词。用户说"你来定"的项，由你根据上下文合理推断。
- **不询问界面参数**：画幅比例（16:9/9:16）、分辨率（720p/480p）、视频时长——这些都是即梦 Seedance 界面上直接可选的参数，**禁止在提示词中写入，也禁止在问答中询问**。提示词只写内容（描述/镜头/表演/声音/禁止项）。
- **时间戳相对化（多片段项目铁律）**：每个提示词是独立生成的视频文件——提示词内所有 `[X-Y秒]` 时间戳**一律从本片段第 1 秒（0s）起算**，禁止用全片绝对时间（如"3-8.5秒"）作为片段内动作时间；片段在全片中的位置（如"全片 3-8.5s"）标注在提示词外（片段说明/文件名），不写入提示词正文。
- **代码块必须带 `txt` 标记**：用 ` ```txt ` 而非裸 ` ``` `——否则无复制按钮。
- **同步输出文件**：提示词同时写入 `outputs/` 目录并用 present_files 展示。
- **参考图防火墙**：提示词中必须声明"参考图仅锁定外观，姿态/动作/机位/表情以正文为准"。
- **默认中文输出**：提示词默认中文——除非用户明确要求英文。
